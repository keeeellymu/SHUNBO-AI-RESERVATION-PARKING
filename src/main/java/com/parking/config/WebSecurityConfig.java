package com.parking.config;

import org.springframework.context.annotation.Configuration;

/**
 * Web安全配置类
 * 注意：当前已完全禁用Spring Security，仅保留为配置类的结构
 */
@Configuration
public class WebSecurityConfig {
    // 移除所有安全相关配置
    // 此配置类仅作为占位符存在
}