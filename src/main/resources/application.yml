spring:
  profiles:
    active: ${APP_ENV:dev}
  main:
    allow-circular-references: true

# 应用基础配置
server:
  port: 8082  # 改为 8082，避免端口冲突
  address: 0.0.0.0  # 监听所有网络接口，允许从其他设备访问
  servlet:
    context-path: /

# 日志配置
logging:
  level:
    root: ${LOG_LEVEL:INFO}
    com.parking: DEBUG